@model IEnumerable<KsiegarniaOnline.Models.PublishHouse>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PublishHouseName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PublishmentYear)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PublishHouseName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublishmentYear)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPublishHouse">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPublishHouse">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPublishHouse">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
